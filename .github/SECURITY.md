# Security and privacy

The local core is stateless. It does not send birth data over the network, call
an LLM, write a profile, or publish content. The optional public MCP endpoint
processes requests in memory and does not persist birth or name input.

Report vulnerabilities privately through GitHub Security Advisories. Do not
include real birth records, names, consultation histories, credentials, or
third-party personal data in a public issue.
